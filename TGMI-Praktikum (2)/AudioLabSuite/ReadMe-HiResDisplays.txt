Die Programme unterstützen nur eine Font-Skalierung von 100%
Bei Hires-Displays ist die Skalierung i.d.R. auf einen anderen Wert gestellt.

Workaround:
Umgebungsvariable QT_AUTO_SCREEN_SCALE_FACTOR anlegen und auf 1 setzen